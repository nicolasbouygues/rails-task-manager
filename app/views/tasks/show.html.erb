<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<%= link_to "Delete", delete_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>

<%= link_to "back to site", index_path  %>
<%= link_to "Edit a task", edit_task_path %>
